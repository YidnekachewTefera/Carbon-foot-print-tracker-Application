setTimeout(() => {
    // Transition to your actual application here (remove splash screen from DOM)
    // splashScreen.parentNode.removeChild(splashScreen);
    window.location.href = "TransportEmission.aspx"; // You can also redirect
}, 19000); // Wait for animation to finish
